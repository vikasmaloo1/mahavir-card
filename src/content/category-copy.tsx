import type { CategoryCopyProps } from "@/components/category-copy";

/**
 * Editorial copy for each category landing page, keyed by public path.
 * Drop copy in here — plain strings or JSX — and it renders under the H1.
 * See src/components/category-copy.tsx for the prop shape.
 */
export const categoryCopy: Record<string, CategoryCopyProps> = {
  // "visiting-card-printing-ahmedabad": {
  //   intro: "…",
  //   sections: [{ heading: "…", body: "…" }],
  // },
};
